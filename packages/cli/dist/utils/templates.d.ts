/**
 * Копирование шаблона в целевую директорию
 * @param template Название шаблона
 * @param destination Целевая директория
 */
export declare function copyTemplate(template: string, destination: string): Promise<void>;
