/**
 * In this exercise, you need to create a Counter component that:
 * 1. Maintains a count state that starts at 0
 * 2. Has buttons to increment and decrement the count
 * 3. Has a button to reset the count to 0
 * 4. Displays the current count
 * 5. Prevents the count from going below 0
 */

import React from 'react';

// TODO: Implement the Counter component
// Use the useState hook to manage the count state
// Add buttons for increment, decrement, and reset
// Display the current count value

export default Counter; 