/**
 * In this exercise, you need to create a Button component
 * that accepts the following props:
 * - text: the button text (required)
 * - onClick: a function to call when the button is clicked (required)
 * - variant: the button style variant ('primary', 'secondary', 'danger')
 *   with 'primary' as the default
 * - disabled: boolean to disable the button (default: false)
 */

import React from 'react';

// TODO: Implement the Button component here
// It should render a button element with the appropriate props
// Apply different styling based on the variant prop

export default Button; 